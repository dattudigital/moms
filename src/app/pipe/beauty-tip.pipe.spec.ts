import { BeautyTipPipe } from './beauty-tip.pipe';

describe('BeautyTipPipe', () => {
  it('create an instance', () => {
    const pipe = new BeautyTipPipe();
    expect(pipe).toBeTruthy();
  });
});
