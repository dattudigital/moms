import { UserActivityPipe } from './user-activity.pipe';

describe('UserActivityPipe', () => {
  it('create an instance', () => {
    const pipe = new UserActivityPipe();
    expect(pipe).toBeTruthy();
  });
});
